#include<iostream>
using namespace std;
int main()
{
    int a,sum=0,i;
    cin>>a;
    for(i=1;i<a;i++)
    {
        if(a%i==0)
        {
            sum=sum+i;
        }
    }
    if(sum>a)
    {
        cout<<"Abundant number";
    }
    else
    {
        cout<<"Not Abundant number";
    }
}
