#include <iostream>
using namespace std;
int main()
{ 
    int num, ctr, value; 
    cin >> num; 
    if(num > 0) 
    { 
        value = num % 10;
        for(ctr = 1; ctr <= 5; ctr++)
        { 
            cout << (value * ctr) << " "; 
            
        } 
        
    } 
    else 
    {
        value = (num % 10) * (-1);
        for(ctr = 1;
        ctr <= 10; ctr++) 
        {
            cout << (value * ctr) << " ";
            } 
        
    }
    return 0;
    }
