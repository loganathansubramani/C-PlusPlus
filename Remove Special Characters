#include <iostream> 
using namespace std; 
int main()
{ 
    string a;
    int b,i;
    cin>>a;
    b=a.length();
    for(i=0;i<b;i++)
    {
        if(isalpha(a[i])||isdigit(a[i]))
        {
            cout<<a[i];
        }
    }
}
