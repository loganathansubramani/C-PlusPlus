#include<iostream>
using namespace std;
int main()
{
    int a,b,c=0;
    cin>>a;
    b=a;
    while(a>0)
    {
       c=c*10;
       c=c+(a%10);
       b=b/10;
    }
       if(a==c)
    {
        cout<<"Same";
    }
    else
    {
        cout<<"Not Same";
    }
    
    
}
