#include<iostream>
#include<string>
#include<sstream>
using namespace std;
int main() {
    string a;
    getline(cin, a);
    stringstream ss(a);
    string c;
    while (getline(ss, c, ',')) {
        cout << c << endl;
    }
    return 0;
}
