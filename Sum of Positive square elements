#include<iostream>
#include<math.h>
using namespace std;
int main()
{
    int a,i,c=0;
    cin>>a;
    int b[a];
    for(i=0;i<a;i++)
    {
        cin>>b[i];
        if(sqrt(b[i])==int(sqrt(b[i])))
        {
            c=c+b[i];
        }
    }
    cout<<c;
}
