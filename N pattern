#include <iostream>
using namespace std; 
int main() 
{
int N; 
char ch;
cin >> N >> ch;
for(int row = 1; row <= N; row++) 
{
for(int col = 1; col <= N; col++) 
{
if(col == 1 || col == N || col == row) 
{
cout << ch; 
}
else 
{
cout << "-"; 
}
}
cout << endl; 
}
return 0; 
}
